# /ledger.build — TDD 实现 + 架构检查

加载 ledger-build skill 执行 TDD 实现。

## 执行

1. 运行 guard：`bash .ledger/bin/ledger.sh guard build`
2. 加载 skill：使用 Skill 工具加载 `ledger:ledger-build`
3. 按 skill 指引执行

guard 失败则停止，不继续。
