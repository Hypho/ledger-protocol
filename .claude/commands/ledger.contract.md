# /ledger.contract — 行为契约生成

加载 ledger-contract skill 执行行为契约生成。

## 执行

1. 运行 guard：`bash .ledger/bin/ledger.sh guard contract`
2. 加载 skill：使用 Skill 工具加载 `ledger:ledger-contract`
3. 按 skill 指引执行

guard 失败则停止，不继续。
