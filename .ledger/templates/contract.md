# 行为契约 — [功能名称]
版本：v1.0 | 关联 PID：.ledger/specs/[功能名]-pid.md | 日期：[日期]

> ⚠️ 体积控制：单文件建议不超过 15,000 字符（约 300 行）。
> 超出时 Claude Code 会自动截断为预览，导致 build / verify 阶段遗漏条目。
> 若功能过大，优先考虑拆分为子功能，而非压缩 FC 描述精度。

## 功能契约（FC）

### 正常路径（关键路径，必须测试）
FC-01：当 [操作] 时，系统返回/显示/写入 [结果]
FC-02：当 [操作] 时，系统返回/显示/写入 [结果]

### 边界与异常（关键路径，必须测试）
FC-03：输入校验 — 当输入为空 / 超长 / 格式非法 / 类型错误时，系统拒绝并返回明确错误
FC-04：状态冲突 — 当 [并发操作 / 重复提交 / 状态不满足前置条件] 时，系统返回 [结果]
FC-05：权限与隔离 — 当 [权限不足 / 跨租户访问 / 越权操作] 时，系统拒绝并返回 [结果]

### 次要异常（标注[可选]，不生成自动化测试）
FC-06：[可选] 当 [低优先级异常] 时，系统返回/显示/写入 [结果]

> 💡 边界条件分组写法：将同类防御逻辑归入同一个 FC 条目。
> 例如"输入校验"覆盖空值、超长、非法格式等多种情况，只需一个 FC-03，无需逐条拆分。
> 一个 FC 条目可以包含多个 When/Then 场景，只要它们属于同一类防御逻辑。

## 非功能契约（NF）

### 可自动化测试
NF-01：当 [明确可触发的条件] 时，系统返回/显示/写入 [表现]

### 依赖环境（人工验收）
NF-02：[需要特殊环境才能测试，如慢网络]

## 测试覆盖范围
自动化：FC-01 ~ FC-0X，NF-01
人工验收：FC-0X（可选），NF-02（依赖环境）

## 明确不做
| 类别 | 内容 | 理由 |
|------|------|------|
| 永不做 | [PID Card 中明确排除的范围] | [超出功能边界] |
| 本版本不做 | [本功能之外的相邻需求] | [当前迭代不涉及] |
| 简化做 | [如有] | [只做基本版] |

## 粒度说明
FC 条目数：[N]
覆盖类别：[Schema / 后端逻辑 / UI / 权限 / 异步任务 / 其他]
结论：[可在一次 build -> verify 闭环完成 / 需拆分 / 需 exec-plan]
